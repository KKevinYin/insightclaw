# Insightclaw MVP 设计文档

日期：2026-05-15

## 目标

Insightclaw 是一个个人精选的 AI Agent 工具指南站。MVP 版本主要帮助不同水平的用户回答三个问题：

- 我该如何开始使用 Agent 工具？
- OpenClaw 和 Claude Code 该怎么安装？
- 不同任务应该选择哪些模型？

网站整体应该像一个干净的知识库：易读、实用、可信，并带有轻量品牌感。它不应该复制 HengClaw 那种霓虹赛博风格。

## 目标用户

网站服务混合人群：

- 需要清晰安装路径和解释的新手用户。
- 想了解实用 Agent 工作流和模型选择的开发者。
- 想看精选推荐，而不是大而全导航的 AI 工具用户。

首页和安装页应该对新手友好。更深入的模型页面可以包含更多技术细节。

## MVP 范围

第一版包含五个页面：

- `/` 首页
- `/install` OpenClaw 和 Claude Code 安装教程
- `/llms` 模型推荐
- `/models` 动态模型排行
- `/about` 站点定位和筛选标准

MVP 不包含登录、评论、CMS、全站搜索、完整工具导航页，也不包含教程文章系统。

## 信息架构

### 首页

首页介绍 Insightclaw 是一个 AI Agent 工具与模型指南。它应该优先帮助用户快速进入下一步，而不是写很长的营销文案。

核心模块：

- 首屏：简洁的品牌说明和主要行动入口。
- 三个入口卡片：安装工具、选择模型、查看排行。
- 推荐路径：分别给新手、开发者和进阶用户一条建议路线。
- 小型精选模块：展示少量精选链接或推荐内容。

### 安装页

安装页覆盖两条路线：

- OpenClaw 路线：环境准备、安装、首次运行、基础使用、常见问题。
- Claude Code 路线：安装方式、登录/鉴权、打开项目、常用工作流、常见问题。

页面可以使用标签页，也可以用两个清晰分隔的章节。安装步骤应该来自结构化数据，这样后续更新内容时不需要重写页面组件。

### 模型推荐页

`/llms` 页面采用混合推荐结构：

- 按用途推荐：编程、中文问答、长上下文、低成本、强推理、日常均衡。
- 按工具推荐：OpenClaw 推荐模型、Claude Code 推荐模型。

每个推荐项都应该说明：

- 适合谁。
- 为什么推荐。
- 有哪些取舍或注意事项。
- 建议使用场景。

### 模型排行页

`/models` 页面使用 OpenRouter 数据生成动态排行。

MVP 功能：

- 模型排行列表或表格。
- 按模型名称或 ID 搜索。
- 免费/付费筛选。
- 按热度/排名、价格、上下文长度排序。
- 展示模型名称、供应商、上下文长度、输入/输出价格，以及可用时的能力标签。

如果 OpenRouter 数据拉取失败，页面应展示本地备用榜单，并用简短提示说明实时数据暂时不可用。

### 关于页

关于页说明：

- Insightclaw 是个人精选指南，不是广告聚合目录。
- 推荐标准基于易用性、稳定性、性价比，以及是否适合真实工作流。
- 未来可能扩展 Agent 工具导航、教程文章和可复用配置模板。

## 内容模型

MVP 阶段不做 CMS，内容放在代码级数据文件中。

建议文件：

```text
data/
  featuredLinks.ts
  fallbackModels.ts
  installGuides.ts
  llmRecommendations.ts
```

这样可以保持第一版简单，同时为后续添加工具导航或文章系统留下空间。

## 技术方向

推荐技术栈：

- Next.js App Router
- TypeScript
- Tailwind CSS
- OpenRouter API，用于模型排行数据
- 大部分内容页使用静态页面
- `/models` 使用服务端或缓存式数据拉取

部署可以先使用 Vercel。MVP 跑通后，再接自定义域名和 Cloudflare 缓存。

## 视觉方向

网站应该采用干净的文档/知识库风格：

- 白色或近白色背景。
- 高可读性字体排版。
- 清晰的区块间距。
- 实用的卡片和表格。
- 克制的品牌强调色。
- 尽量少装饰。

避免重霓虹渐变、深色赛博视觉，以及过度营销化的落地页布局。

## 错误处理

OpenRouter 拉取失败不应该导致页面崩溃。

预期行为：

- 展示备用模型数据。
- 显示一个轻量、不打扰的状态提示。
- 搜索、筛选、排序在备用数据上仍然可用。

## 测试与验证

MVP 验证项：

- 五个路由都能正常渲染。
- 导航链接可用。
- 安装页在移动端和桌面端都易读。
- `/llms` 推荐卡片方便浏览，内容不溢出。
- `/models` 在可用时能加载 OpenRouter 实时数据。
- `/models` 在实时数据失败时能使用备用数据。
- 模型搜索、筛选、排序功能可用。

## 后续扩展

MVP 之后可以考虑：

- `/tools` 或 `/agents` 工具目录。
- `/guides/[slug]` 教程文章系统。
- 跨工具和教程的搜索功能。
- 模型对比详情页。
- OpenClaw 和 Claude Code 的可复用配置模板。
- Newsletter 或更新订阅。

